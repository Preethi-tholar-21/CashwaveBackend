server:
  port: 9091 # Port for the admin-service

spring:
  application:
    name: admin-service # Name of the service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/ # URL of the Eureka Server
